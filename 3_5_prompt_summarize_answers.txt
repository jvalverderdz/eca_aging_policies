I will give you a CSV file with the following structure: Source, for different countries, question, for 47 different questions, rank, chunk_id, score, and text. I want you to do the following: For each country: Read the questions in 'question' and then the answer given to such question in 'text'. There are 10 answers ('text's) per question per country ('source').

You will summarize the answer to each question for each country from the 'text' registries such that the question in 'question' is best answered for that country. Don't use anything outside the 'text' for your reasoning, but summarize it so it best answers the question. If the answer to the question is not in the text, declare it by starting the answer with "NO"; if it is in the text, start with "YES"; if it is only partially, start with "PARTIALLY". You should build your own paraphrases such that the answer makes grammatical sense. Answer should be around 500 characters (counting spaces), and strictly less than 600 characters.

Create a new CSV file with the following structure: 'country' with the country in 'source', and two columns per question: one is the question itself, such that the cell of 'country'*'question' is the summary you created, and one with the name 'source' (not to confuse with 'source' in the input file, this is different), such that it has a list of the unique 'chunk_id's for that specific 'country'*'question'.

Therefore, I should have a CSV file with a table of 22 rows: one per country; and 95 columns: one for 'country', one for the answer to each question and one for the source of such answer (list of unique 'chunk_id's).

Additional considerations: There are additional columns in the CSV I will give you: rank, score. You should ignore these, and just summarize 'text'. Some 'text' answers are chunks of an index or table of contents. You can ignore these. Clean the text so it only has gramatically sound phrases. Don't use Anthropic API, but read them yourself. Too avoid overloading the context window, do this process by steps: First, iterate over each country, in each iteration: read and write the dictionaries that will form the CSV into a TXT file (one per country); second, implement a python code to read the TXTs and integrate them into a DataFrame; finally, in that same python code, export such DataFrame as a CSV.